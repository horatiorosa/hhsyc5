<p id="notice"><%= notice %></p>

 <!-- Page Content -->
    <div class="container">

        <!-- Page Heading/Breadcrumbs -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">HHSYC - Administrators View
                    <!-- <small>founded in 2001</small> -->
                </h1>
                <ol class="breadcrumb">
                    <li><%=link_to 'Admin Home Page', adminfisher_path %>
                    </li>  
                    <li> 
                    <%= link_to "New Event or Announcement", new_announcement_path, class: "active" %>
                    </li>
                     <li> 
                    <%= link_to "View Announcements", announcements_path, class: "active" %>
                    </li>
                    <li>  
                    <%= link_to "New Blog Post", announcements_path, class: "active" %>
                    </li> 
                    <li> 
                    <%= link_to "New Photo for Gallery", announcements_path, class: "active" %>
                    </li>
                     <ul>
                          <li class="go-right">
                              
                              <%= link_to "Logout", destroy_admin_session_path, class: "active", method: :delete %>
                              
                          </li>
                     </ul>
                    
                  
                </ol>
            </div>
        </div>
        <!-- /.row -->

        <!-- Content Row -->
        <div class="row">
            <!-- Sidebar Column -->
                <%= render 'shared/sidebar_column' %>
            <!-- Content Column -->
            <div class="col-md-9">
              <div class="stylestatic">
                <h3>Announcements Listing</h3>
                <table>
                  <thead>
                    <tr>
                      <th colspan="3"></th>
                    </tr>
                  </thead>

                    <tbody>
                      <% @announcements.each do |announcement| %>
                        <tr>
                          <td><%= link_to announcement.ann_title, announcement %></td>
                          <td><%= link_to 'View', announcement %></td>
                          <% if admin_signed_in? %>
                          <td><%= link_to 'Edit', edit_announcement_path(announcement) %></td>
                          <td><%= link_to 'Delete', announcement, method: :delete, data: { confirm: 'Are you sure?' } %></td><% end %>
                        </tr>
                      <% end %>
                    </tbody>
                </table>
              </div>  
            </div>
        </div>
        <!-- /.row -->

        <hr>

