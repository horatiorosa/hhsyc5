<% if admin_signed_in? %>

<p id="notice"><%= notice %></p>

 <!-- Page Content -->
    <div class="container">

        <!-- Page Heading/Breadcrumbs -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">HHSYC - Administrators View
                    <!-- <small>founded in 2001</small> -->
                </h1>
                <ol class="breadcrumb">
                    <li><%=link_to 'Admin Home Page', adminfisher_path %>
                    </li>  
                    <li> 
                    <%= link_to "New Event or Announcement", new_announcement_path, class: "active" %>
                    </li>
                     <li> 
                    <%= link_to "View Announcements", announcements_path, class: "active" %>
                    </li>
                    <li>  
                    <%= link_to "New Blog Post", announcements_path, class: "active" %>
                    </li> 
                    <li> 
                    <%= link_to "New Photo for Gallery", announcements_path, class: "active" %>
                    </li>
                     <ul>
                          <li class="go-right">
                              
                              <%= link_to "Logout", destroy_admin_session_path, class: "active", method: :delete %>
                              
                          </li>
                     </ul>
                    
                  
                </ol>
            </div>
        </div>
        <!-- /.row -->

        <!-- Content Row -->
        <div class="row">
            <!-- Sidebar Column -->
                <%= render 'shared/sidebar_column' %>
            <!-- Content Column -->
            <div class="col-md-9">
              <div class="stylestatic">
                <h3>Editing Announcement</h3>

									<%= render 'form' %>

									<%= link_to 'Show', @announcement %> |
									<%= link_to 'Back', announcements_path %>

              </div>  
            </div>
        </div>
        <!-- /.row -->

        <hr>
<% else %>

<%= get '/401', :to => redirect('/401.html') %>

<% end %>


